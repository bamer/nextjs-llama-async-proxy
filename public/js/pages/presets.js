/**
 * Presets Page - Barrel Export
 *
 * DEPRECATED: The functionality has been split into multiple files.
 * This file is kept for backwards compatibility.
 *
 * New code should import from:
 * - ./presets/presets-controller.js
 * - ./presets/presets-page.js
 * - ./presets/presets-editor.js
 */

// Assign to window for backwards compatibility (no ES modules)
if (typeof window !== "undefined") {
  // Controllers are already assigned in their respective files
  // This file just provides a barrel import point
}
